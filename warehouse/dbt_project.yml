name: dbt_semantic_mcp
version: "0.1.0"
profile: dbt_semantic_mcp

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  dbt_semantic_mcp:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      +materialized: table
    utilities:
      +materialized: table
